# Core runtime dependencies for the FSA / position-bias characterization study.
# Pinned to versions tested with Python 3.12.2.
# For a stricter lockfile, run `pip freeze > requirements-frozen.txt` from a clean venv.

openai==1.12.0
python-dotenv==1.0.1
httpx==0.27.0
tenacity==8.2.3

# Analysis and visualization
numpy==2.3.5
pandas==2.3.3
matplotlib==3.9.2
seaborn==0.13.2

# Web app (Streamlit dashboard for transcript inspection)
streamlit==1.54.0

# NLP utilities (used in light text processing)
nltk==3.9.1
scikit-learn==1.6.1

# Image generation (figures)
Pillow>=10.0.0
